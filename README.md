# Inventory-Manager
Dragon's Dogma 2 Inventory Management Mod

## REFramework project layout

```text
reframework/
├── autorun/
│   └── Inventory-Manager/
├── data/
│   └── Inventory-Manager/
├── fonts/
├── images/
└── plugins/
```

Use `Inventory-Manager` as this mod's `<modname>` folder inside `autorun` and `data`.
